    <section>
      <div class="row">
        <div class="col-sm-8 col-sm-offset-2">
          <div class="product_single">
            <h1>Create a new nerdy product:</h1>

            <%= form_tag '/products', method: :post do %>
              <%= render 'form' %>
              <%= submit_tag "Process New Nerdiness", class: 'btn btn-primary' %>
            <% end %>
          </div>
        </div>
      </div>
    </section>
